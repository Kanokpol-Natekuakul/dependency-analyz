import { add, multiply } from './utils/math';
import { formatDate } from '../dependency-analyzer/test-project/utils/date';
import { Button } from '../dependency-analyzer/test-project/components/Button';

console.log(add(1, 2));
console.log(formatDate(new Date()));
